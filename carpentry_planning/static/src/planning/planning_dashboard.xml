<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <div t-name="carpentry_planning.PlanningDashboardCard" owl="1" class="card m-2" t-att-class="props.className">
        <div class="card-body">
            <t t-if="props.slots.title"> 
                <h4 class="card-title mb-2"><t t-slot="title"/></h4>
            </t>
            <p class="card-text"><t t-slot="default"/></p>
        </div>
    </div>

    <div t-name="carpentry_planning.PlanningDashboard" owl="1" class="o_carpentry_planning_dashboard p-1">
        <div class="d-flex flex-wrap">
            <t t-foreach="items" t-as="item" t-key="item_index">
                <PlanningDashboardCard>
                    <t t-set-slot="title">
                        <i t-attf-class="fa {{ item.icon }} me-2" />
                        <t t-esc="item.title" />
                    </t>
                    <t t-call="carpentry_planning.PlanningDashboard.{{ item.id }}" />
                </PlanningDashboardCard>
            </t>
        </div>
    </div>

    <!-- Planning Dashboard Card: next projects -->
    <t t-name="carpentry_planning.PlanningDashboard.next_projects" owl="1">
        <div t-foreach="data.next_projects" t-as="assigment" t-key="assigment.id" class="mt-2">
            <a class="cursor-pointer" t-on-click="() => this.openNextProject(assigment.project_id[0])">
                <t t-esc="assigment.user_id[1]" />
            </a>
            (<t t-esc="assigment.role_id[1]" />)
            
            <br />
            <a class="cursor-pointer" t-on-click="() => this.openNextProject(assigment.project_id[0])">
                <t t-esc="assigment.project_id[1]" />
            </a>
        </div>
    </t>

</templates>
